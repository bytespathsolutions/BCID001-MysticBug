MONGO_URI=mongodb+srv://<user>:<pass>@cluster.../dbname
PORT=5000

# Option A (recommended): Put the full service account JSON string
# EXACT JSON stringified (escape newlines) or one-line JSON
FIREBASE_SERVICE_ACCOUNT='{"type":"service_account", ... }'
 